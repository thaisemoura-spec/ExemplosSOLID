// CalculadoraSalario.java (Nova classe para cálculo)
class CalculadoraSalario {
    public double calcular(Funcionario funcionario) {
        System.out.println("Cálculo de salário para " + funcionario.getNome() + " realizado.");
        return 0.0;
    }
}